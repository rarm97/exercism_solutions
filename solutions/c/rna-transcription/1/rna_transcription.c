#include "rna_transcription.h"
#include <stdio.h>
#include <string.h>

char *to_rna(const char *dna) {
  int value = strcmp(dna, dna);
  if (value == 0) {
    printf("Value %i", value);
  }
  return "";
}
// Slur
